{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c \"chmod -R a+rwX /data; if [ ! -f /data/config.yaml ]; then printf 'terminal:\\n  backend: modal\\n  modal_mode: direct\\n' > /data/config.yaml; fi; exec hermes gateway run\"",
    "healthcheckPath": null,
    "restartPolicyType": "ON_FAILURE"
  }
}
