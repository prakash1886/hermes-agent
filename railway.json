{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c \"chmod -R a+rwX /data; printf '%s\\n' 'terminal:' '  backend: modal' '  modal_mode: direct' > /data/config.yaml; cat /data/config.yaml; exec hermes gateway run\"",
    "healthcheckPath": null,
    "restartPolicyType": "ON_FAILURE"
  }
}
